<Page
    x:Class="kicquwp.InfoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:kicquwp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:ConnectionId='2' x:Name="LayoutRoot">

        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--Панель заголовка-->
        <StackPanel Grid.Row="0" Margin="19,0,0,0">
            <TextBlock Text="kicq4WP" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,12,0,0"/>
            <TextBlock Text="Справка" Margin="0,-6.5,0,26.5" Style="{ThemeResource HeaderTextBlockStyle}" CharacterSpacing="{ThemeResource PivotHeaderItemCharacterSpacing}"/>
        </StackPanel>

        <!-- Оборачиваем TextBlock в ScrollViewer, если текста может быть много -->
        <ScrollViewer Grid.Row="1" Margin="19,9.833,19,10" VerticalScrollBarVisibility="Auto">
            <TextBlock x:ConnectionId='5' x:Name="Info"
                       FontSize="25"
                       Foreground="White"
                       TextWrapping="Wrap"
                       Text="kicq4WP — это клиент ICQ для устройств на базе Windows Phone 8.1, реализующий протокол OSCAR. Клиент позволяет удобно общаться в сети ICQ, поддерживая основные функции мессенджера." />
        </ScrollViewer>
        <TextBlock x:ConnectionId='3' x:Name="author"
       FontSize="20"
       Foreground="White"
       Margin="19,466.833,19,0"
       VerticalAlignment="Top"
       Text="Создатель: blackblur13 (AKA. inklime)" Grid.Row="1" />
        <!--TODO: Содержимое должно размещаться в следующей сетке-->
        <Grid x:ConnectionId='4' Grid.Row="1" x:Name="ContentRoot" Margin="19,9.5,19,0">
            <!-- здесь можно разместить дополнительный контент -->
        </Grid>
    </Grid>
</Page>

